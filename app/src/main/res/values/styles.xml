<resources>
    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Splash screen theme for Android 12 and above -->
    <style name="SplashTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/colorPrimary</item> <!-- Background color -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item> <!-- Optional: Icon to display -->
        <item name="windowSplashScreenIconSize">48dp</item> <!-- Size of the icon -->
        <item name="windowSplashScreenDuration">2000</item> <!-- Duration of splash screen (in milliseconds) -->
    </style>
</resources>
